<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog-Node.js</h1>
  <p class="lead">Aqui você poderá criar, editar e apagar postagens, assim como as suas respectivas categorias</p>
  <hr class="my-4">
  
  <a class="btn btn-primary btn-lg" href="#" role="button">Criar conta</a>
</div>

<hr/>

<h2>Postagens recentes:</h2>

{{#each postagens}}
  <div class="card">
    <div class="card-body">
      <h3>{{titulo}}</h3>
      <p>{{descricao}}</p>
      <a href="#"><button class="btn btn-success">Leia mais</button></a>
      <hr/>
      <small>{{categoria.nome}}</small><br>
      <small>Data de publicação: {{categoria.data}}</small>
    </div>
  </div>
{{else}}
  <h5>Nenhuma postagem</h5>
{{/each}}